{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <div class="container" dir="rtl">
        <h2>مرحبا بك</h2>
        <p>تمتع بالخدمات البلدية و ساهم في تحديد الخيــــارات المنــــاسبة لمنطقتك عن بعد!</p>

        <div class="row">
            <div class="col-md-3">&nbsp;</div>
            <div class="col-md-6">
                <div class="bg-light py-3 px-3">
                    <h1> تسجيل الدخول</h1>
                    
                    <p></p>
                    {% if hasError %}
                        <div class="alert alert-danger">
                            الرجاء ملء كل الفراغات والتثبت من البريد الالكتروني وكلمة السر 
                        </div>
                    {% endif %}
                    <form action="{{ path('account_login')}}" method="post">
                        <div class="form-group">
                            <label for="email">البريد الالكتروني </label>
                            <input type="text" name="_username" id="email" class="form-control" placeholder=" البريد" required value="{{username}}">
                        </div>
                        <div class="form-group">
                            <label for="password">كلمة العبور </label>
                            <input type="password" name="_password" id="password" placeholder="*****************" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-success">
                                الدخول
                            </button>
                        </div>
                    </form>
                </div>

            </div>
        </div>

    </div>
{% endblock %}
